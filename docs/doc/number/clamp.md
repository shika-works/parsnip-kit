# clamp
[[[desc clamp
]]]
[[[desc clamp zh
  以最大值 `max`，最小值 `min`，截断入参 `num`。
]]]
[[[desc clamp ja
  最大値 `max` を上限、最小値 `min` を下限として、入力パラメータ `num` をクランプする。
]]]
[[[version clamp
  
]]]

### Usage

[[[demo]]]


### API

#### Arguments

[[[params clamp

]]]
[[[params clamp zh
num: 要截断的数值
min: 最小值
max: 最大值
]]]
[[[params clamp ja
num: クランプする数値
min: 最小値
max: 最大値
]]]

#### Returns

[[[returns clamp

]]]
